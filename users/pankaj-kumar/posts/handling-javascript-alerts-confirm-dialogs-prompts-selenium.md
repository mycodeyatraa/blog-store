---
title: Handling JavaScript Alerts, Confirm Dialogs, and Prompts in Selenium
date: 13-Jul-2024
lastUpdated: 11-Jun-2026
author: pankaj-kumar
authorName: Pankaj Kumar
authorRole: Automation Architect
authorAvatar: https://raw.githubusercontent.com/mycodeyatraa/blog-store/main/users/pankaj-kumar/images/pankaj.JPG
authorBio: Automation Architect
authorGithub: https://github.com/pankajhyd
authorLinkedin: https://www.linkedin.com/in/pankaj-kumar-94a2b227/
tags: [selenium, java, alerts, confirm-dialog, prompts, overlays]
category: UI Automation
categories: [UI Automation, Selenium, Java]
excerpt: >-
  A complete guide to handling browser native dialogs in Selenium WebDriver. Learn how to switch to, accept, dismiss, and input text into alerts, confirms, and prompts in Java.
readTime: 7 min read
---

# Handling JavaScript Alerts, Confirm Dialogs, and Prompts in Selenium

> 📅 **Last Updated:** 11-Jun-2026

Native browser alerts, confirmation boxes, and prompt boxes pose a unique challenge for automated testing. Because these dialogs are generated by the browser operating system shell rather than the HTML web engine, they do not exist inside the traditional DOM tree. As a result, standard locators like XPath, CSS Selectors, or ID tags will not work.

In this tutorial, we will explore how to intercept, validate, input data, and dismiss browser native alerts using Selenium WebDriver's `Alert` interface in Java.

---

## 🧭 Native Alert Interaction Flow

Since native dialogs freeze browser page execution, they must be handled immediately. The sequence diagram below shows how the WebDriver protocol switches execution threads from the standard HTML document context to the browser's operating system alert loop:

![diagram_1](https://raw.githubusercontent.com/mycodeyatraa/blog-store/main/users/pankaj-kumar/posts/handling-javascript-alerts-confirm-dialogs-prompts-selenium/images/diagram_1.png)

---

## 🛠️ Step-by-Step Code Walkthrough

Below is the complete TestNG implementation. It launches a headless browser, navigates to the overlays portal, and handles:
1. **Simple Alerts**: Reading message content and clicking "OK" (`.accept()`).
2. **Confirmation Dialogs**: Clicking "OK" (`.accept()`) and clicking "Cancel" (`.dismiss()`).
3. **Prompt Dialogs**: Injecting a custom text payload using `.sendKeys()` before accepting.

```java
package com.mycodeyatra.tests;
import io.github.bonigarcia.wdm.WebDriverManager;
import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.Assert;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;
import java.time.Duration;
public class AlertTest {
    private WebDriver driver;
    private WebDriverWait wait;
    @BeforeMethod
    public void setUp() {
        WebDriverManager.chromedriver().setup();
        ChromeOptions options = new ChromeOptions();
        options.addArguments("--remote-allow-origins=*");
        options.addArguments("--headless=new");
        options.addArguments("--window-size=1920,1080");
        driver = new ChromeDriver(options);
        driver.manage().window().maximize();
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
        wait = new WebDriverWait(driver, Duration.ofSeconds(5));
    }
    @Test
    public void testJavaScriptAlerts() {
        System.out.println("Navigating to: https://practice.mycodeyatra.com/");
        driver.get("https://practice.mycodeyatra.com/");
        System.out.println("Opening Sandbox Arena...");
        WebElement sandboxLink = wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//span[contains(text(),'Sandbox Arena')]")));
        sandboxLink.click();
        System.out.println("Opening Alerts & Overlays Page...");
        WebElement overlaysTile = wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//h3[text()='Alerts & Overlays']")));
        overlaysTile.click();
        // 1. Native Alert Handler
        System.out.println("Testing Standard JavaScript Alert...");
        driver.findElement(By.xpath("//button[@data-testid='alert-btn']")).click();
        Alert alert = wait.until(ExpectedConditions.alertIsPresent());
        String alertText = alert.getText();
        System.out.println("Alert Text Captured: " + alertText);
        Assert.assertEquals(alertText, "This is a standard JavaScript Alert!");
        alert.accept();
        WebElement resultText = driver.findElement(By.xpath("//div[@data-testid='alert-result']"));
        System.out.println("Result Text: " + resultText.getText());
        Assert.assertTrue(resultText.getText().contains("Alert dismissed"));
        // 2. Native Confirm Handler - Accept (OK)
        System.out.println("Testing Confirm Alert - Clicking OK...");
        driver.findElement(By.xpath("//button[@data-testid='confirm-btn']")).click();
        alert = wait.until(ExpectedConditions.alertIsPresent());
        Assert.assertEquals(alert.getText(), "Do you want to proceed?");
        alert.accept();
        System.out.println("Result Text: " + resultText.getText());
        Assert.assertTrue(resultText.getText().contains("You clicked OK"));
        // 3. Native Confirm Handler - Dismiss (Cancel)
        System.out.println("Testing Confirm Alert - Clicking Cancel...");
        driver.findElement(By.xpath("//button[@data-testid='confirm-btn']")).click();
        alert = wait.until(ExpectedConditions.alertIsPresent());
        alert.dismiss();
        System.out.println("Result Text: " + resultText.getText());
        Assert.assertTrue(resultText.getText().contains("You clicked Cancel"));
        // 4. Native Prompt Handler
        System.out.println("Testing Prompt Alert - Sending Input...");
        driver.findElement(By.xpath("//button[@data-testid='prompt-btn']")).click();
        alert = wait.until(ExpectedConditions.alertIsPresent());
        Assert.assertEquals(alert.getText(), "Please enter your name:");
        alert.sendKeys("Pankaj");
        alert.accept();
        System.out.println("Result Text: " + resultText.getText());
        Assert.assertTrue(resultText.getText().contains("Hello, Pankaj!"));
        System.out.println("JavaScript alerts test validations complete!");
    }
    @AfterMethod
    public void tearDown() {
        if (driver != null) {
            System.out.println("Quitting driver session...");
            driver.quit();
        }
    }
}
```

---

## 💻 Test Execution Logs

Below are the console outputs of the test execution, confirming standard alert triggers and validations:

```bash
[INFO] Running com.mycodeyatra.tests.AlertTest
Navigating to: https://practice.mycodeyatra.com/
Opening Sandbox Arena...
Opening Alerts & Overlays Page...
Testing Standard JavaScript Alert...
Alert Text Captured: This is a standard JavaScript Alert!
Result Text: Result: Alert dismissed
Testing Confirm Alert - Clicking OK...
Result Text: Result: You clicked OK
Testing Confirm Alert - Clicking Cancel...
Result Text: Result: You clicked Cancel
Testing Prompt Alert - Sending Input...
Result Text: Result: Hello, Pankaj!
JavaScript alerts test validations complete!
Quitting driver session...
Tests run: 1, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.753 sec
```

---

## 📊 Summary of Alert Class Interactions

The Selenium `Alert` interface provides four primary commands. Understanding their support across different dialog types prevents syntax errors:

| Action Method | Simple Alert (`alert()`) | Confirm Box (`confirm()`) | Prompt Box (`prompt()`) |
| :--- | :--- | :--- | :--- |
| `getText()` | Returns dialog message | Returns dialog message | Returns dialog message |
| `accept()` | Clicks "OK" (dismisses) | Clicks "OK" (confirms) | Clicks "OK" (submits text) |
| `dismiss()` | Clicks "OK" (same as accept) | Clicks "Cancel" (rejects) | Clicks "Cancel" (cancels input) |
| `sendKeys(text)` | Ignored (throws warning/no-op) | Ignored (throws warning/no-op) | Type character array into field |

---

## ⚠️ Common Pitfalls

* **UnhandledAlertException**: If an alert is triggered in the browser background, any subsequent standard element locator call (like `driver.findElement`) will fail, throwing an `UnhandledAlertException`. You must handle or close active dialogs before continuing DOM queries.
* **NoAlertPresentException**: Invoking `driver.switchTo().alert()` before the browser has finished rendering the pop-up thread throws a `NoAlertPresentException`. Always wrap switches inside explicit waits (`ExpectedConditions.alertIsPresent()`).
* **Visualizing SendKeys in Headless Mode**: When running tests in headless Chrome, the input string sent via `.sendKeys()` into a prompt field will *not* be physically rendered on the window. However, clicking `.accept()` will successfully submit the value to the page context.

---

## 🙋 Frequently Asked Questions

> **Q: How can we close custom dialog modals that are built with HTML?**
>
> **A:** Custom CSS modal overlays (like Bootstrap or React overlays) are part of the standard HTML DOM. Do *not* use `driver.switchTo().alert()`. Instead, locate the close button container (using XPath or CSS) and trigger a standard `.click()`.

> **Q: Can we handle HTTP Basic Authentication login prompts using the Alert API?**
>
> **A:** No. Browser authentication pop-ups (username/password login grids) are security barriers and cannot be switched to via the standard `Alert` client. Handle them by injecting credentials directly into the URL schema: `https://admin:admin123@practice.mycodeyatra.com/`.
