# Blog Store

Public data repository for MyCodeYatra blog articles. Populated automatically by the MCP server.

- `users/{author}/posts/` — article markdown files
- `users/{author}/images/` — article cover images
- `registry.json` — master index of all articles
